App\Entity\User:
  attributes:
    id:
      groups: ['user:item:get']
    email:
      groups: ['user:item:get', 'user:collection:post']
    username:
      groups: ['user:item:get', 'user:collection:post']
    password:
      groups: ['user:item:get']
    customer:
      groups: ['user:item:get', 'user:collection:post']